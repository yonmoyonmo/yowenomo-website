{
  "name": "crpg-dungeon",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node esbuild.config.mjs --servedir=public --watch",
    "build": "node esbuild.config.mjs",
    "gen:assets": "node scripts/gen_assets.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.20.2",
    "typescript": "^5.4.5"
  }
}
